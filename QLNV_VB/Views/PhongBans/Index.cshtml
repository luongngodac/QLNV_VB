@model IEnumerable<QLNV_VB.Models.PhongBan>

@{
    ViewBag.Title = "PHÒNG BAN";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>PHÒNG BAN</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
             @*@@Html.DisplayNameFor(model => model.TenPB)*@
            Tên Phòng Ban
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenPB)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id = item.MaPB }) |
            @Html.ActionLink("Chi tiết", "Details", new { id = item.MaPB }) |
            @Html.ActionLink("Xóa", "Delete", new { id = item.MaPB })
        </td>
    </tr>
}

</table>
